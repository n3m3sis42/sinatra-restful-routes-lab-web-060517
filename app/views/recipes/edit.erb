<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Create New Recipe</title>
  </head>
  <body>
    <h1>Create New Recipe</h1>
    <form action="/recipes/<%= @recipe.id %>" method="POST">
      <input id="hidden" type="hidden" name="_method" value="patch">
      <p><label>Name </label><input type="text" id="name" name="recipe[name]" value=<%= "#{@recipe.name}" %> /></p>
      <p><label>Ingredients </label><input type="text" id="ingredients" name="recipe[ingredients]" value=<%= "#{@recipe.ingredients}" %> /></p>
      <p><label>Cook Time </label><input type="text" id="cook_time" name="recipe[cook_time]" value=<%= "{@recipe.cook_time}" %> /></p>

      <p><input type="submit" value="submit"></p>
    </form>
  </body>
</html>
